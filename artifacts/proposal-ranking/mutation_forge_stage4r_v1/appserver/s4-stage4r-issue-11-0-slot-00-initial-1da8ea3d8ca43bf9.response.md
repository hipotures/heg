{"schema_version":"stage4.generated_policy.v1","source":"def priority(ctx, proposal):\n    counts = ctx[\"capped_cycle_counts\"]\n    broken = proposal[\"broken_sampled_witnesses_by_length\"]\n    loads = proposal[\"removed_edge_load_sum_by_length\"]\n    n = min(16, max(1, len(counts)))\n    coverage = 0.0\n    load = 0.0\n    for i in range(16):\n        if i < n:\n            c = min(1000000, max(0, counts[i]))\n            b = min(1000000, max(0, broken[i]))\n            l = min(1000000, max(0, loads[i]))\n            weight = 1.0 + c / (1.0 + c)\n            coverage = coverage + weight * b / (1.0 + b)\n            load = load + weight * l / (1.0 + l)\n    coverage = coverage / (2.0 * n)\n    load = load / (2.0 * n)\n    triangle = min(1000000, max(0, proposal[\"local_triangle_risk\"]))\n    c4 = min(1000000, max(0, proposal[\"local_c4_risk\"]))\n    risk = (triangle + c4) / (1.0 + triangle + c4)\n    return coverage + 0.15 * load - 0.02 * risk","design_summary":"Ranks candidates primarily by bounded sampled-witness coverage, with current witness counts supplying modest length weighting. Adds a stronger but secondary bounded removed-load signal and a small local-risk penalty.","change_summary":"Removes concentration, distance, and alias-derived structural terms; drops the redundant peak signal; increases the load contribution from a negligible tie-breaker to a secondary coefficient and keeps risk explicitly bounded.","hypothesis":"Unique sampled witness coverage should be the most reliable early improvement proxy, while removed-edge load can break useful ties or improve targeting. Local triangle and C4 risk should receive a limited penalty without overwhelming witness reduction.","used_fields":["ctx.capped_cycle_counts","proposal.broken_sampled_witnesses_by_length","proposal.removed_edge_load_sum_by_length","proposal.local_triangle_risk","proposal.local_c4_risk"],"assumptions":["Aligned context and proposal vectors have at least one entry and no more than 16 entries.","Larger bounded witness coverage and removed-edge load generally indicate better candidate rewrites.","Local triangle and C4 risk are useful directional penalties despite their bounded-budget caveats."],"expected_failure_modes":["Sampled witnesses may be unrepresentative, causing high coverage or load to miss unsampled cycles.","Load sums can reward repeated hits on overlapping witnesses rather than broad coverage.","Incomplete local-risk budgets may understate reconnection risk.","The pool-constant count weighting cannot distinguish candidates except by modulating their candidate signals."]}