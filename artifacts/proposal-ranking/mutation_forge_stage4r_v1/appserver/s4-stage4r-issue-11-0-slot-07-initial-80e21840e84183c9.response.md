{"schema_version":"stage4.generated_policy.v1","source":"def priority(ctx, proposal):\n    counts = ctx['capped_cycle_counts']\n    broken = proposal['broken_sampled_witnesses_by_length']\n    loads = proposal['removed_edge_load_sum_by_length']\n    breadth = 0\n    product = 1\n    weighted = 0\n    for i in range(16):\n        signal = 0\n        if i < len(broken):\n            signal += broken[i]\n        if i < len(loads):\n            signal += loads[i]\n        if i < len(counts):\n            signal *= 1 + min(counts[i], 8)\n        if signal > 0:\n            breadth += 1\n            product *= 1 + min(signal, 24)\n            weighted += signal\n    risk = proposal['local_triangle_risk'] + 2 * proposal['local_c4_risk']\n    separation = proposal['minimum_distance_between_removed_edges'] + proposal['mean_distance_between_removed_edges']\n    remoteness = proposal['minimum_preexisting_distance_for_new_edges'] + proposal['mean_preexisting_distance_for_new_edges']\n    score = product * 100 + breadth * 1000 + weighted * 3 + separation * 2 + remoteness * 2\n    score -= risk * risk * 20\n    score -= proposal['k'] * 3\n    if ctx['recent_duplicate_rate'] > 0.5:\n        score += separation + remoteness\n    if ctx['remaining_steps'] <= 4:\n        score -= risk * 15\n    return score","design_summary":"Uses a nonlinear breadth-first witness composition: capped per-length witness signals are multiplied across covered forbidden lengths, with current witness counts acting as pressure weights. Structural separation and reconnection remoteness provide secondary tie-breaking, while quadratic local-risk penalties suppress fragile rewrites.","change_summary":"Replaces the parent’s linear coverage/load score and fixed low-risk k bonus with a multiplicative multi-length coverage objective, explicit distance terms, complexity regularization, and context-sensitive duplicate/late-search risk adjustments.","hypothesis":"Candidates that make useful contact across several currently burdened forbidden lengths should be more reliable than candidates concentrating signal in one length. Moderate separation and remoteness may improve rewrite quality, but nonlinear local-risk penalties should prevent those secondary heuristics from selecting structurally dangerous moves.","used_fields":["ctx.capped_cycle_counts","ctx.recent_duplicate_rate","ctx.remaining_steps","proposal.broken_sampled_witnesses_by_length","proposal.removed_edge_load_sum_by_length","proposal.local_triangle_risk","proposal.local_c4_risk","proposal.minimum_distance_between_removed_edges","proposal.mean_distance_between_removed_edges","proposal.minimum_preexisting_distance_for_new_edges","proposal.mean_preexisting_distance_for_new_edges","proposal.k"],"assumptions":["Breadth of positive sampled witness contact across configured lengths is a useful robustness proxy.","Current capped witness counts can safely amplify candidate signals at corresponding lengths.","Distance features are useful only as secondary heuristics because their sentinels and graph-distance interpretation are imperfect.","Quadratic local triangle/C4 risk is an appropriate conservative penalty.","High recent duplicate rate justifies a modest preference for separated and remote rewrites.","Near the end of a search trajectory, additional risk aversion is preferable to aggressive exploration."],"expected_failure_modes":["Multiplicative coverage may overvalue shallow positive signals across many lengths over a very strong single-length intervention.","Sampled witness proxies may miss the true forbidden cycles or favor repeated coverage of nonrepresentative samples.","Distance sentinel values may falsely reward proposals affected by distance-budget exhaustion.","Quadratic risk penalties may reject genuinely beneficial rewrites with unavoidable local risk.","The context-sensitive adjustments may misread caller history or reduce useful exploration late in a run."]}