# Superseded benchmark micro-20260723T235832Z

Operation timings are valid, but the generic peak-RSS field inherited the
launcher's historical `ru_maxrss`. The same JSON records the authoritative
cgroup peak of 19,922,944 bytes. A replacement report uses cgroup
`memory.peak` as the primary field.

- Created: 2026-07-23T23:58:32Z
- Kind: microbenchmark
- Peak RSS: 263139328 bytes

Raw samples and p50/p90/p95/max values are preserved in the JSON report.

The C++ subprocess is retained as an independent verifier; whether it is
used in a ranking loop must be decided from the measured Python/C++ timings.
